# The server imports mcp.server.mcpserver, which exists only in mcp 2.0 — the old
# floor of >=1.0.0 would happily install a 1.x that cannot run this code at all.
mcp>=2.0.0,<3

# Semantic search: numpy holds the vector matrix and does the similarity
# multiply; openai is the client for the GPUStack embedding endpoint.
numpy>=1.24
openai>=1.30

# server.py imports uvicorn directly to put the auth gate in front of the ASGI
# app, so it is a direct dependency now. It arrives with mcp today; a transitive
# dependency that an upstream release drops is a failure at start-up, not at
# install, and this project has already paid for that once.
uvicorn>=0.30
